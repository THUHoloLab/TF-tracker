hydra:
  run:
    dir: /data/cyt2/deep_tracker/${model.name}/${now:%Y-%m-%d_%H%M%S}

gt_path: /data/cyt2/deep_tracker/gt_tracks/gt_tracks/
running_locally: False


weights_path: /data/cyt2/TF_tracker_3/checkpoints/epoch=epoch=4349-step=step=95700.ckpt

#/data/cyt2/deep_tracker/pretrained_weights/pretrained_weights/weights_finetuned_ec.ckpt

track_name: shitomasi_custom
representation: time_surfaces_v2_5
patch_size: 31 # 31
visualize: True
save_frames: False
dt_track_vis: 0.2

# Composing nested config with default
defaults:
  - model: correlation3_unscaled
# Pytorch lightning trainer's argument
trainer:
  gpus: [0]
